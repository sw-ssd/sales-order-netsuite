## Spec Changes

UI component change only — no product capability specifications are added or modified.

- **Affected files**: columns.tsx in 8 admin datatable pages
- **Change type**: Checkbox → Button replacement in `rud_acts` column header
