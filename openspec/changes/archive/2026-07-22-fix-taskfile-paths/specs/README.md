## Spec Changes

This change is purely a tooling/build configuration fix — no product capability specs are added or modified.

- **Affected files**: `sales-order-app/Taskfile.yml` (path fix), `Taskfile.yml` (verification only)
- **No spec changes required**: All fixes are in build/dev-tooling configuration, not product behavior.
